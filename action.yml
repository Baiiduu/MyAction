name: "Echo Environment Variables"
description: "Echo selected GitHub Actions environment variables."

inputs:
  extra-env-names:
    description: "Comma-separated environment variable names to echo in addition to the defaults."
    required: false
    default: ""

runs:
  using: "composite"
  steps:
    - name: Echo environment variables
      shell: bash
      run: |
        echo "GITHUB_ACTION=$GITHUB_ACTION"
        echo "GITHUB_ACTION_PATH=$GITHUB_ACTION_PATH"
        echo "GITHUB_ACTOR=$GITHUB_ACTOR"
        echo "GITHUB_BASE_REF=$GITHUB_BASE_REF"
        echo "GITHUB_EVENT_NAME=$GITHUB_EVENT_NAME"
        echo "GITHUB_HEAD_REF=$GITHUB_HEAD_REF"
        echo "GITHUB_JOB=$GITHUB_JOB"
        echo "GITHUB_REF=$GITHUB_REF"
        echo "GITHUB_REF_NAME=$GITHUB_REF_NAME"
        echo "GITHUB_REPOSITORY=$GITHUB_REPOSITORY"
        echo "GITHUB_RUN_ATTEMPT=$GITHUB_RUN_ATTEMPT"
        echo "GITHUB_RUN_ID=$GITHUB_RUN_ID"
        echo "GITHUB_RUN_NUMBER=$GITHUB_RUN_NUMBER"
        echo "GITHUB_SHA=$GITHUB_SHA"
        echo "GITHUB_WORKFLOW=$GITHUB_WORKFLOW"
        echo "GITHUB_WORKSPACE=$GITHUB_WORKSPACE"
        echo "RUNNER_ARCH=$RUNNER_ARCH"
        echo "RUNNER_NAME=$RUNNER_NAME"
        echo "RUNNER_OS=$RUNNER_OS"
        echo "RUNNER_TEMP=$RUNNER_TEMP"
        echo "RUNNER_TOOL_CACHE=$RUNNER_TOOL_CACHE"

        if [ -n "${{ inputs.extra-env-names }}" ]; then
          echo "${{ inputs.extra-env-names }}" | tr ',' '\n' | while IFS= read -r name; do
            name="$(echo "$name" | xargs)"
            if [ -n "$name" ]; then
              echo "$name=${!name}"
            fi
          done
        fi
